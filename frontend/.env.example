# Backend API base URL — no trailing slash, no /api suffix
# The backend serves routes directly at /auth/login, /organizations/, etc.
VITE_API_URL=http://localhost:8000

# Application Name
VITE_APP_NAME=Secure Fair

# Environment
VITE_ENVIRONMENT=development
